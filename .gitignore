*.code-workspace

node_modules
*.log

# Demo (Vite) build output — keep ignored; the package's root dist/ is tracked intentionally
demo/dist/

# superpowers SDD scratch (ledger, briefs, review packages) — never committed
.superpowers/
